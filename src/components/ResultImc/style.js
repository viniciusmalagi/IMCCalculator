import { StyleSheet, View } from 'react-native';

const styles = StyleSheet.create({
    resultImc: {
        flex: 1,
        width: '100%',
        marginTop: 15,
        paddingTop: 60,
        borderRadius: 50,
        alignItems: 'center',
    },
    information: {
        fontSize: 18,
        color: '#ff043',
        fontWeight: 'bold',
    },
    numberImc: {
        fontSize: 50,
        color: '#ff043',
        fontWeight: 'bold',
    },
    level: {
        fontSize: 40,
        color: '#ff043',
        fontWeight: 'bold',
        marginTop: 30,
    },
  });

export default styles
